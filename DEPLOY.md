# Инструкция по деплою проекта

## 🚀 Варианты бесплатного хостинга

### 1. GitHub Pages (рекомендуется, если уже настроен домен)

**Плюсы:**
- ✅ Бесплатно
- ✅ Уже настроен домен
- ✅ Автоматический деплой через GitHub Actions
- ✅ HTTPS автоматически

**Минусы:**
- ⚠️ Только статические файлы (подходит для Flutter Web)
- ⚠️ Ограничения по размеру (достаточно для MVP)

#### Настройка:

1. **Включите GitHub Pages в настройках репозитория:**
   - Settings → Pages
   - Source: `Deploy from a branch`
   - Branch: `gh-pages` (будет создана автоматически)
   - Folder: `/ (root)`

2. **Настройте домен (если еще не настроен):**
   - Settings → Pages → Custom domain
   - Введите ваш домен
   - Добавьте CNAME файл (создастся автоматически)

3. **Пушьте код:**
   ```bash
   git add .
   git commit -m "Setup deployment"
   git push origin main
   ```

4. **GitHub Actions автоматически:**
   - Соберет проект
   - Задеплоит в `gh-pages` ветку
   - Обновит сайт

#### Важно для base-href:

Если ваш сайт доступен по `yourdomain.com/bikes/`, в `.github/workflows/deploy.yml` используйте:
```yaml
flutter build web --release --base-href "/bikes/"
```

Если в корне домена `yourdomain.com`, используйте:
```yaml
flutter build web --release
```

---

### 2. Netlify (альтернатива, очень просто)

**Плюсы:**
- ✅ Очень простой деплой
- ✅ Автоматический деплой из Git
- ✅ Бесплатный SSL
- ✅ Поддержка кастомных доменов
- ✅ Preview deployments для PR

**Минусы:**
- ⚠️ Нужно подключить репозиторий

#### Настройка:

1. Зайдите на [netlify.com](https://netlify.com)
2. Sign up через GitHub
3. New site from Git → выберите репозиторий
4. Build settings:
   - **Build command:** `flutter build web --release`
   - **Publish directory:** `build/web`
5. Добавьте кастомный домен в настройках сайта

#### Netlify конфигурация (опционально):

Создайте `netlify.toml`:
```toml
[build]
  command = "flutter build web --release"
  publish = "build/web"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
```

---

### 3. Vercel (альтернатива)

**Плюсы:**
- ✅ Отличная производительность
- ✅ Автоматический деплой
- ✅ Edge functions (если понадобятся)
- ✅ Бесплатный SSL

**Минусы:**
- ⚠️ Нужно подключить репозиторий

#### Настройка:

1. Зайдите на [vercel.com](https://vercel.com)
2. Sign up через GitHub
3. Import Project → выберите репозиторий
4. Build settings:
   - **Framework Preset:** Other
   - **Build Command:** `flutter build web --release`
   - **Output Directory:** `build/web`
5. Добавьте кастомный домен

---

### 4. Cloudflare Pages (альтернатива)

**Плюсы:**
- ✅ Отличная CDN
- ✅ Бесплатный SSL
- ✅ Поддержка кастомных доменов
- ✅ Очень быстрый

**Минусы:**
- ⚠️ Нужно подключить репозиторий

#### Настройка:

1. Зайдите в Cloudflare Dashboard
2. Pages → Create a project
3. Connect to Git → выберите репозиторий
4. Build settings:
   - **Framework preset:** None
   - **Build command:** `flutter build web --release`
   - **Build output directory:** `build/web`
5. Добавьте кастомный домен

---

## 🔧 Локальная сборка для тестирования

Перед деплоем проверьте локально:

```bash
# Установите зависимости
flutter pub get

# Соберите веб версию
flutter build web --release

# Запустите локальный сервер для тестирования
cd build/web
python3 -m http.server 8000
# или
npx serve .

# Откройте http://localhost:8000
```

---

## 📝 Настройка для вашего домена

### Если домен в корне (yourdomain.com):

1. В `.github/workflows/deploy.yml`:
   ```yaml
   flutter build web --release
   ```

2. В `web/index.html` проверьте:
   ```html
   <base href="/">
   ```

### Если домен в подпапке (yourdomain.com/bikes/):

1. В `.github/workflows/deploy.yml`:
   ```yaml
   flutter build web --release --base-href "/bikes/"
   ```

2. В `web/index.html`:
   ```html
   <base href="/bikes/">
   ```

---

## 🐛 Решение проблем

### Проблема: Белый экран после деплоя

**Решение:**
- Проверьте `base-href` в build команде
- Проверьте пути к assets в `pubspec.yaml`
- Убедитесь, что все файлы задеплоены

### Проблема: 404 на роутах

**Решение:**
- Добавьте `_redirects` файл (для Netlify) или настройте редиректы
- Для GitHub Pages: все роуты должны вести на `index.html`

### Проблема: PWA не работает

**Решение:**
- Убедитесь, что сайт работает по HTTPS
- Проверьте `manifest.json` и пути к иконкам
- Проверьте Service Worker

---

## 🎯 Рекомендация

Для вашего случая (уже есть домен и GitHub Pages):

1. **Используйте GitHub Pages** - уже настроен домен
2. Настройте GitHub Actions (файл уже создан)
3. При каждом пуше в `main` будет автоматический деплой

Если нужна более простая настройка:
- **Используйте Netlify** - очень простой интерфейс, автоматический деплой

---

## 📊 Сравнение вариантов

| Параметр | GitHub Pages | Netlify | Vercel | Cloudflare |
|----------|--------------|---------|--------|------------|
| Бесплатно | ✅ | ✅ | ✅ | ✅ |
| Кастомный домен | ✅ | ✅ | ✅ | ✅ |
| SSL | ✅ | ✅ | ✅ | ✅ |
| Автодеплой | ✅ | ✅ | ✅ | ✅ |
| Простота | ⭐⭐⭐ | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐ | ⭐⭐⭐ |
| Скорость | ⭐⭐⭐ | ⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ |

---

*Выберите вариант и следуйте инструкциям выше*

