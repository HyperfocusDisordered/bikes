# Interactive Project Tree

Интерактивный инструмент для разработки проекта Bikes.

## Запуск

### ⚡ Быстрый запуск (рекомендуется)
```bash
cd dev-tools/interactive
./start.sh
```

Сервер запустится из корня проекта, откройте:
**http://localhost:8000/dev-tools/interactive/index.html**

### Вариант 2: Вручную из корня проекта
```bash
# Из корня проекта bikes/
python3 -m http.server 8000
```

Затем откройте: http://localhost:8000/dev-tools/interactive/index.html

### Вариант 3: Live Server в VS Code
1. Установите расширение "Live Server"
2. Откройте корень проекта (bikes/) в VS Code
3. Правый клик на `dev-tools/interactive/index.html` → "Open with Live Server"

## Возможности

- 🌳 **Интерактивное дерево проекта** - слева
- 👁️ **Превью кода** - справа при наведении/клике
- 🎨 **UI Preview** - для экранов и компонентов
- 🔍 **Поиск** - быстрый поиск по дереву
- 🎯 **Подсветка** - активные элементы выделяются

## 🔄 Live Reload

Сервер автоматически отслеживает изменения в следующих файлах:
- `project-data.json`
- `code-links.json`
- `preview.html`
- `index.html`

При изменении любого из этих файлов все открытые страницы автоматически перезагружаются.

**⚠️ ВАЖНО для AI-ассистентов:**
После внесения изменений в файлы `project-data.json`, `code-links.json`, `preview.html` или `index.html` **НЕ нужно** вручную обновлять страницу в браузере - сервер автоматически отправит команду на перезагрузку всем подключенным клиентам через Server-Sent Events.

## ⚠️ ВАЖНО: Поддержка актуальности project-data.json

**Для AI-ассистентов и разработчиков:**

Файл `project-data.json` является **источником истины** для структуры проекта и используется интерактивным инструментом для визуализации архитектуры приложения.

### Обязательные действия при изменениях:

1. **При добавлении новых экранов/компонентов:**
   - Добавьте их в соответствующий раздел `📱 app` в `project-data.json`
   - Опишите навигацию и связи между экранами

2. **При изменении состояния (state):**
   - Обновите раздел `💾 state` в `project-data.json`
   - Добавьте новые поля и их типы

3. **При добавлении новых API-эндпоинтов:**
   - Добавьте их в раздел `🌐 api` с описанием метода, параметров и шагов

4. **При изменении Bluetooth-функций:**
   - Обновите раздел `📶 bluetooth` с новыми командами и шагами

5. **При добавлении утилит:**
   - Добавьте в раздел `🔧 utils` с описанием входных/выходных параметров

6. **При изменении действий (actions):**
   - Обновите раздел `⚡ actions` с новыми действиями и их шагами

### Правила обновления:

- ✅ **Всегда** обновляйте `project-data.json` синхронно с изменениями кода
- ✅ Используйте эмодзи-нотацию для визуальной идентификации (📱, 💾, 🌐, 📶, 🔧, ⚡)
- ✅ Описывайте связи между компонентами через стрелки (→)
- ✅ Указывайте условия через `if` и `then`
- ✅ Документируйте шаги выполнения функций в массиве `steps`

**Файл должен отражать актуальное состояние проекта в любой момент времени!**

## Использование

1. Откройте страницу в браузере
2. Наведите на элемент в дереве - увидите превью
3. Кликните - загрузится полный код
4. Для UI элементов справа показывается описание компонента

## Интеграция с Cursor

Можно открыть в отдельном окне браузера рядом с Cursor для удобной навигации по проекту.

