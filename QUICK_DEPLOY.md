# 🚀 Быстрый деплой на GitHub Pages

## Шаг 1: Настройте GitHub Actions

Файл `.github/workflows/deploy.yml` уже создан. Проверьте:

1. **Измените base-href** в зависимости от вашего пути:
   - Если сайт в корне: `flutter build web --release`
   - Если в подпапке `/bikes/`: `flutter build web --release --base-href "/bikes/"`

2. **Проверьте ветку** (main или master):
   ```yaml
   branches:
     - main  # или master
   ```

## Шаг 2: Включите GitHub Pages

1. Зайдите в Settings → Pages вашего репозитория
2. Source: `Deploy from a branch`
3. Branch: `gh-pages` (будет создана автоматически)
4. Folder: `/ (root)`
5. Save

## Шаг 3: Настройте домен (если еще не настроен)

1. В Settings → Pages → Custom domain
2. Введите ваш домен (например: `bikes.yourdomain.com`)
3. Сохраните

## Шаг 4: Запушьте код

```bash
git add .
git commit -m "Setup GitHub Pages deployment"
git push origin main
```

## Шаг 5: Проверьте деплой

1. Зайдите в Actions вкладку GitHub репозитория
2. Дождитесь завершения workflow
3. Откройте ваш сайт!

---

## ⚡ Альтернатива: Netlify (еще проще)

Если GitHub Pages не подходит:

1. Зайдите на [netlify.com](https://netlify.com)
2. Sign up через GitHub
3. New site from Git → выберите репозиторий
4. Build settings:
   - Build command: `flutter build web --release`
   - Publish directory: `build/web`
5. Deploy!

Netlify автоматически подключит ваш кастомный домен.

---

## 🔍 Проверка перед деплоем

```bash
# Локальная сборка
flutter build web --release

# Проверка результата
cd build/web
python3 -m http.server 8000
# Откройте http://localhost:8000
```

---

**Готово!** После первого пуша в main, сайт автоматически задеплоится.

